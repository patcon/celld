# The Cloudflare Sandbox SDK image: a control server on port 3000 that the
# `@cloudflare/sandbox` package drives. Keep the tag and the package version
# on the same release line.
FROM docker.io/cloudflare/sandbox:0.12.9
EXPOSE 3000
